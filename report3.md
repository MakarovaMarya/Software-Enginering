# Тема 3. Операторы, условия, циклы
Отчет по Теме #3 выполнил(а):
* Макарова Мария Александровна 
* ИВТ-23-1

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |
| Задание 6 | + |  |
| Задание 7 | + |  |
| Задание 8 | + |  |
| Задание 9 | + |  |
| Задание 10 | + |  |

знак "+" - задание выполнено; знак "-" - задание не выполнено;

Работу проверили:
* Ротенштрайх Т. В.

# Лабораторная работа
## Задание №1
Создайте две переменные, занчение которых будете вводить через консоль. Также составьте условие, в котором созданные ранее переменные будут сравниваться, если условие выполняется, то выведете в консоль «Выполняется», если нет, то «Не выполняется».
```python
one = int(input("Введите значение первой переменной: "))
two = int(input("Введите значение второй переменной: "))
if one >= two:
    print("Выполняется")
else:
    print("Не выполняется")
```

## Результат
<img width="629" height="501" alt="image" src="https://github.com/user-attachments/assets/6de9f0e5-c62a-42a0-8b8f-417ac136c568" />

## Вывод
С помощью input ввели значения для двух переменных в консоль, а затем с помощью условия проверили, является ли первое число больше второго или нет, и вывели результат.

## Задание №2
Напишите программу, которая будет определять значения переменной меньше 0, больше 0 и меньше 10 или больше 10. Это нужно реализовать при помощи одной переменной, значение которой будет вводится через консоль, а также при помощи конструкций if, elif, else.
```python
one = int(input("Введите значение переменной: "))
if one < 0:
    print('Переменная меньше 0')
elif 0 < one < 10:
    print('Переменная больше 0 и меньше 10')
else:
    print('Переменная больше 10')
```

## Результат
<img width="568" height="523" alt="image" src="https://github.com/user-attachments/assets/9aedcc7a-21b7-429f-b1ed-b74b816260a0" />


## Вывод
С помощью input ввели значение переменной, а затем с помощью конструкций if, elif, else проверили, является ли введенное число меньше 0, больше 0, но меньше 10, или больше 10.

## Задание №3
Напишите программу, в которой будет проверяться есть ли переменная в указанном массиве используя логический оператор in. 
Самостоятельно посмотрите, как работает программа со значениями которых нет в массиве numbers.
```python
numbers = [1, 3, 4, 6, 8, 9]
value = int(input('Введите значение переменной: '))
if value in numbers:
    print('Переменная есть в данном массиве')
else:
    print('Переменной нет в данном массиве')
```

## Результат
<img width="567" height="504" alt="image" src="https://github.com/user-attachments/assets/34bd5b26-0aeb-4d75-9624-84e8eb4b145d" />

## Вывод
Создали массив, с помощью input ввели значение искомой переменной в консоль. Через условие с помощью оператора in проверили, есть ли заданная переменная в массиве или нет, и вывели результат.

## Задание №4
Напишите программу, которая будет определять находится ли
переменная в указанном массиве и если да, то проверьте четная она или нет. Самостоятельно протестируйте данную программу с разными
значениями переменной value.
```python
numbers = [1, 3, 4, 6, 8, 9, 15, 16, 73, 321, 322]
value = int(input('Введите значение переменной: '))
if value in numbers:
    if value % 2 == 0:
        print('Переменная четная и есть в массиве numbers')
    else:
        print('Переменная нечетная и есть в массиве numbers')
else:
    print(f"Переменной нет в массиве numbers и она равна {value}")
```

## Результат 
<img width="700" height="565" alt="image" src="https://github.com/user-attachments/assets/c55f0cbf-faeb-4e19-86ab-0f0fae89be47" />

## Вывод
Создали массив, с помощью input ввели значение искомой переменной в консоль. Через условие с помощью оператора in проверили, есть ли заданная переменная в массиве или нет. Если искомая переменная есть в массиве, с помощью условия проверили, является ли она четной или нечетной. В конце вывели результат.

## Задание №5
Напишите программу, в которой циклом for значения переменной i
будут меняться от 0 до 10 и посмотрите, как разные виды сравнений и операций работают в цикле.
```python
for i in range(10):
    print('i = ', i)
    if i == 0:
        i += 2
    if i == 1:
        continue
    if i == 2 or i == 3:
        print('Переменная равна 2 или 3')
    elif i in [4, 5, 6]:
        print('Переменная равна 4, 5 или 6')
    else:
        break
```

## Результат
<img width="528" height="904" alt="image" src="https://github.com/user-attachments/assets/5197a29d-606c-4ca6-846f-da3c668c2f6a" />

## Вывод
С помощью цикла for посмотрели, как разные виды сравнений и операций работают в цикле.

## Задание №6
Напишите программу, в которой при помощи цикла for определяется есть ли переменная value в строке string и посмотрите, как работает оператор else для циклов. Самостоятельно посмотрите, что выведет программа, если значение переменной value оказалось в строке string. 
```python
string = 'Привет всем изучающим Python!'
value = input()
for i in string:
    if i == value:
        index = string.find(value)
        print(f"Буква '{value}' есть в строке под {index} индексом")
        break
else:
    print(f"Буквы '{value}' нет в указанной строке")
```

## Результат
<img width="726" height="584" alt="image" src="https://github.com/user-attachments/assets/4cdb3955-90c5-4fb4-9b65-43026cf7ea30" />

## Вывод
Написали строку, затем с помощью input ввели искомую букву, далее с помощью цикла for и условия проверили, есть ли икомая буква в строке. Если буква нашлась, прерываем цикл и выводим ответ. Если цикл дошел до конца строки и буква не была найдена, выводим сообщение о том, что этой буквы в строке нет. 

## Задание №7
Напишите программу, в которой вы наглядно посмотрите, как работает цикл for проходя в обратном порядке, то есть, к примеру не от 0 до 10, а от 10 до 0. В уже готовой программе показано вычитание из 100, а вам во время реализации программы будет необходимо придумать свой вариант применения обратного цикла.
```python
value = 100
for i in range(10, -1, -1):
    value -= i
    print(i, value)
```

## Результат
<img width="399" height="648" alt="image" src="https://github.com/user-attachments/assets/5c9e2b1c-6587-4d54-b868-91445b67cc39" />

## Вывод
Ввели переменную value, равную 100. Затем прошли по циклу for в обратном порядке, каждый раз выводя разность между текущим значением value и i.

## Задание №8
Напишите программу используя цикл while, внутри которого есть какие-либо проверки, но быть осторожным, поскольку циклы while при неправильно написанных условиях могут становится бесконечными.
```python
value = 0
while value < 100:
    if value == 0:
        value += 10
    elif value // 5 > 1:
        value *= 5
    else:
        value -= 5
    print(value)
```

## Результат
<img width="375" height="568" alt="image" src="https://github.com/user-attachments/assets/bc2944fc-a8fc-4407-b201-716025a6e67d" />

## Вывод
С помощью цикла while и использования различных условий преобразовали значение 0 в значения 10, 50, 250. Цикл остановился, т.к. значение value стало больше 100.

## Задание №9
Напишите программу с использованием вложенных циклов и одной проверкой внутри них. Самое главное, не забудьте, что нельзя использовать одинаковые имена итерируемых переменных, когда вы используете вложенные циклы.
```python
value = 0
for i in range(10):
    for j in range(10):
        if i != j:
            value += j
        else:
            pass
print(value)
```

## Результат
<img width="375" height="506" alt="image" src="https://github.com/user-attachments/assets/f3b5f849-7ed2-4695-85e1-600168735977" />

## Вывод
Использовали два вложенных цикла, а затем с помощью условия проверили, равны ли значения в двух циклах или нет. Если значения не равны, добавляем к value значение во втором цике. Также мы использовали pass - оператор-заглушку, чтобы проверить работу кода на текущем этапе, не придумывая действие для else.

## Задание №10
Напишите программу с использованием flag, которое будет определять есть ли нечетное число в массиве. В данной задаче flag выступает в роли индикатора встречи нечетного числа в исходном массиве, четных чисел.
```python
even_array = [2, 4, 6, 8, 9]
flag = False
for value in even_array:
    if value % 2 == 1:
        flag = True

if flag is True:
    print('В массиве есть нечетное число')
else:
    print('В массиве все числа четные')
```

## Результат
<img width="518" height="557" alt="image" src="https://github.com/user-attachments/assets/1e71e225-80cd-40e8-b590-52a2277588c8" />

## Вывод
Используя флаг как индикатор встречи нечетного числа, мы прошлись по массиву через цикл и с помощью условия находим в массиве нечетное кисло. При проверке, если элемент массива делится на 2 с остатком, мы меняем значение флага с False на True, что означает, что мы нашли нечетное число. Далее с помощью условия определяем, каким будет вывод при значении флага True или False.

# Самостоятельная работа
## Задание №1
Напишите программу, которая преобразует 1 в 31.
Для выполнения поставленной задачи необходимо обязательно и только один раз использовать:
- Цикл for
- *= 5
- += 1
Никаких других действий или циклов использовать нельзя.
```python
num = 1
for i in range(2):
    num *= 5
    num += 1
print(num)
```

## Результат
<img width="322" height="454" alt="image" src="https://github.com/user-attachments/assets/1c4ed0fe-594d-417e-b244-fdc46448a901" />

## Вывод
Для того, чтобы из числа 1 получить 31, нужно последовательно выполнить действия: (num * 5) + 1, а затем повторить те же действия с получившимся числом ещё 1 раз, из чего следует, что цикл for должен сработать 2 раза. 

## Задание №2
Напишите программу, которая фразу «Hello World» выводит в обратном порядке, и каждая буква находится в одной строке консоли.
При этом необходимо обязательно использовать любой цикл, а также программа должна занимать не более 3 строк в редакторе кода.
```python
text = "Hello World"
for char in reversed(text):
    print(char)
```

## Результат
<img width="391" height="617" alt="image" src="https://github.com/user-attachments/assets/dbf032ee-b492-4203-9a2f-a3c02aee5ae3" />

## Вывод
С помощью цикла for мы прошли по строке и вывели каждый её символ, а для того, чтобы выводить буквы в обратном порядке, использовали встроенную функцию reversed. 

## Задание №3
Напишите программу, на вход которой поступает значение из консоли, оно должно быть числовым и в диапазоне от 0 до 10 включительно (это необходимо учесть в программе). Если вводимое число не подходит по требованиям, то необходимо вывести оповещение об этом в консоль и остановить программу. Код должен вычислять в каком диапазоне находится полученное число. Нужно учитывать три диапазона:
- от 0 до 3 включительно
- от 3 до 6
- от 6 до 10 включительно
Результатом работы программы будет выведенный в консоль диапазон. Программа должна занимать не более 10 строчек в редакторе кода.
```python
num = int(input('Введите число в диапазоне от 0 до 10 включительно: '))
if 0 <= num <= 3:
    print('Это число из диапазона от 0 до 3 включительно')
elif 3 < num < 6:
    print('Это число из диапазона от 3 до 6')
elif 6 <= num <= 10:
    print('Это число из диапазона от 6 до 10 включительно')
else:
    print('Ваше число не подходит по требованиям!')
```

## Результат
<img width="777" height="557" alt="image" src="https://github.com/user-attachments/assets/6fc71cb4-a18c-4045-bbf0-78ef436db679" />

## Вывод
Запрашиваем у пользователя число в диапазоне от 0 до 10 включительно. Далее с помощью условий проверяем, к какому из диапазонов подходит введенное число и выводим ответ. Если же число не подходит ни к одному из диапазонов, выводим предупреждение о том, что пользователь ввел неподходящее число. 

## Задание №4
Манипулирование строками. Напишите программу на Python, которая принимает предложение (на английском) в качестве входных данных от пользователя. Выполните следующие операции и отобразите результаты:
- Выведите длину предложения.
- Переведите предложение в нижний регистр.
- Подсчитайте количество гласных (a, e, i, o, u) в предложении.
- Замените все слова "ugly" на "beauty".
- Проверьте, начинается ли предложение с "The" и заканчивается ли на "end".
Проверьте работу программы минимум на 3 предложениях, чтобы охватить проверку всех поставленных условий.
```python
s = input("Введите предложение на английском: ")
print(f"Длина предложения: {len(s)}")
print(f"В нижнем регистре: {s.lower()}")
vowels = sum(1 for char in s.lower() if char in 'aeiou')
print(f"Количество гласных: {vowels}")
new_s = s.replace('ugly', 'beauty').replace('Ugly', 'Beauty')
print(f"После замены 'ugly' на 'beauty': {new_s}")
starts = s.startswith('The')
ends = s.endswith('end')
print(f"Начинается с 'The': {starts}")
print(f"Заканчивается на 'end': {ends}")
```

## Результат
<img width="856" height="712" alt="image" src="https://github.com/user-attachments/assets/fd2e3e68-fdef-4819-bb40-3eab67009dd1" />
<img width="855" height="711" alt="image" src="https://github.com/user-attachments/assets/235d973c-5a76-4747-8cf7-380fcc0123de" />
<img width="856" height="710" alt="image" src="https://github.com/user-attachments/assets/5881c13c-7911-4817-9393-f39e7ae59274" />

## Вывод
Сначала мы запросили у пользователя ввести строку на английском, затем с помощью функции len нашли длину введенной строки, с помощью lower перевели все буквы в строке в нижний регистр, с помощью replace заменили слово "ugly" на "beauty", с помощью startswith и endswith указали слова, с которых может начинаться и заканчиваться строка, а в строке sum(1 for char in s.lower() if char in 'aeiou') посчитали количество символов из списка гласных 'aeiou'. 

## Задание №5
Составьте программу, результатом которой будет данный вывод в консоль.
Программу нужно составить из данных фрагментов кода.
```python
counter = 0
while counter != 10:
    string = 'hello'
    memory = ' world'
    values = [0, 2, 4, 6, 8, 10]
    if counter in values:
        string = string + ' world'
    print(string)
    counter += 1

if counter > 7:
    print(string + memory)
```

## Результат
<img width="455" height="840" alt="image" src="https://github.com/user-attachments/assets/46bf1ace-faf5-4bf6-8e55-271e44895280" />

## Вывод
Сначала мы инициализировали переменную counter значением 0, затем с помощью цикла while повторили действия 10 раз (пока counter не станет равен 10). В каждой итерации цикла мы создавали переменную string со значением 'hello' и memory со значением ' world', а также определяли список values с четными числами от 0 до 10. С помощью условия if counter in values проверяли, находится ли текущее значение счетчика в списке values, и если это условие выполнялось, то дополняли строку string добавлением ' world'. После этого с помощью функции print выводили текущее значение string на каждой итерации цикла. По завершении цикла с помощью условия if counter > 7 проверяли, превышает ли счетчик значение 7, и если это условие выполнялось, выводили concatenated строку string + memory.

# Общий вывод
В этой лабораторной работе я научилась работать с операторами (такими как in), условиями (if, elif, else) и цикламы (while, for), а также с некоторыми встроенными функциями, например reversed и lower. Благодаря использованию вышеперечисленных функций Python я научилась решать конкретные задачи, которые могут встретится в будущем. 
